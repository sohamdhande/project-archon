apiVersion: v1
kind: Service
metadata:
  name: archon-service
  namespace: archon
  labels:
    app: archon
spec:
  selector:
    app: archon
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
